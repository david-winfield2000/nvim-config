# Language Support

Language are set up with the following:

- `mason.nvim` for downloading LSPs
- `blink.cmp` for autocompletion
- `lsp/` for putting lsp server configs

Steps:

1. Use `:Mason` to install LSP/formatters/linters
2. Mason-lspconfig handles the rest
